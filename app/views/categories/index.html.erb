<h1><%= t('.title')%></h1>


 <%= render partial: 'category', collection: @categories %>
 <%= link_to t('.new'), new_category_path, class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium" %>
