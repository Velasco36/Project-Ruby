<div id="<%= dom_id category %>">
  <%= category.name%>

  <%= link_to t('.edit'), edit_category_path(category)%>
   <%= button_to 'Eliminar producto', category_path(category), method: :delete, data: {
    data_confirm: '¿estas seguro que deseas eliminarlo?'
    } %>
</div>
